/*
  Backend endpoint configuration.

  Local Cloudflare Worker example:
    window.WTG_BACKEND_URL = "http://127.0.0.1:8787";

  Deployed Cloudflare Worker example:
    window.WTG_BACKEND_URL = "https://YOUR-WORKER-NAME.YOUR-SUBDOMAIN.workers.dev";

  Leave it empty to keep the dashboard working fully in browser-only mode.
*/
window.WTG_BACKEND_URL = "https://wtg-aaw-backend.mohamedlasheen2019.workers.dev";
